// import React from 'react';

// const MenuItem = ({ item }) => {
//   // Check if the item is undefined or null
//   if (!item) {
//     return <li>Item not available</li>;  // Return a fallback if item is undefined
//   }

//   return (
//     <li>
//       {/* <h3>{item.name}</h3> Display item name */}
//       <p>{item.description}</p> {/* Display item description */}
//       <p>Price: ${item.price ? item.price.toFixed(2) : 'N/A'}</p> {/* Ensure price is formatted */}
//       <img src={item.food_image} alt={item.name} style={{ width: '100px', height: '100px' }} /> {/* Display item image */}
//     </li>
//   );
// };

// export default MenuItem;
